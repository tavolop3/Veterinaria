include ../mixins/bannerCliente.pug

doctype html

+bannerCliente()
html
  head
    title Cruza
  body
    form(action="/clientes/cargar-cruza", method="GET")
        button(type="submit") Cargar cruza
    form(action="/clientes/listar-cruza", method="GET")
        button(type="submit") Listar cruzas